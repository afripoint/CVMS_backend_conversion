# HTTPS Server (SSL)
server {
    listen 443 ssl;
    server_name afridev.com.ng www.afridev.com.ng 89.117.37.128;

    # Full chain certificate and private key
    ssl_certificate      /etc/nginx/ssl/afridev_com_ng_combined.crt;
    ssl_certificate_key  /etc/nginx/ssl/afridev_com_ng.key;

    # Strong SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
    ssl_session_cache shared:SSL:10m;

    # Proxy to Django backend
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        access_log off;
    }

    # Media files
    location /media/ {
        alias /app/media/;
        expires 30d;
        access_log off;
    }
}

# HTTP → HTTPS Redirect
server {
    listen 80;
    server_name afridev.com.ng www.afridev.com.ng 89.117.37.128;
    return 301 https://$host$request_uri;
}
