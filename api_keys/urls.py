from django.urls import path
# from .views import GenerateAPIKeyView, RevokeAPIKeyView, ListAPIKeysView

urlpatterns = [
    # path("api-keys/generate/", GenerateAPIKeyView.as_view(), name="generate-api-key"),
    # path("api-keys/revoke/", RevokeAPIKeyView.as_view(), name="revoke-api-key"),
    # path("api-keys/", ListAPIKeysView.as_view(), name="list-api-keys"),
]